# System Configuration
HOST=127.0.0.1

# Backend Configuration
BACKEND_PORT=666
BACKEND_URL=http://localhost:666

# Frontend Configuration
FRONTEND_PORT=8888
# Note: VITE_ prefix is required for client-side access in Vite
VITE_API_URL=/api/v1
